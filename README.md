





## Setup

This project depends on an editable install of hydromt-sfincs.
Clone and install it (in your environment) before running anything:

    git clone https://github.com/Deltares/hydromt_sfincs.git
    pip install -e hydromt_sfincs
